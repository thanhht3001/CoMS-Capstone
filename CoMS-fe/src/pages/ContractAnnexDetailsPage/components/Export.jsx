import React from 'react';

const Export = () => {
  const handleExport = () => {
    // Logic for exporting data
  };

  return (
    <div class="w-full sm:w-auto flex justify-end mt-4 sm:mt-0">
        <button class="btn btn-primary shadow-md ml">Export</button>
    </div>
  );
};

export default Export;
